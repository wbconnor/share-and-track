<%= form_with(model: acttivity, local: true) do |form| %>
  <% if acttivity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(acttivity.errors.count, "error") %> prohibited this acttivity from being saved:</h2>

      <ul>
      <% acttivity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.check_box :quantity %>
  </div>

  <div class="field">
    <%= form.label :weight %>
    <%= form.check_box :weight %>
  </div>

  <div class="field">
    <%= form.label :time %>
    <%= form.check_box :time %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
